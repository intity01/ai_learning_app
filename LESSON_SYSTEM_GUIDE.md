# 🎓 ระบบจัดการบทเรียนตามระดับและภาษา

## 📋 โครงสร้างระบบ

### 1. **LessonManager** (`lib/services/lesson_manager.dart`)
Service หลักสำหรับจัดการบทเรียนตามระดับและภาษา

**Features:**
- ✅ รองรับ 4 ภาษา: JP, EN, CN, KR
- ✅ รองรับหลายระดับต่อภาษา
- ✅ โหลดบทเรียนจาก JSON files
- ✅ สร้างบทเรียนอัตโนมัติจาก API (fallback)

**ระดับที่รองรับ:**
- **JP (ภาษาญี่ปุ่น):** N5, N4, N3, N2, N1
- **EN (ภาษาอังกฤษ):** Beginner, Intermediate, Advanced
- **CN (ภาษาจีน):** HSK1, HSK2, HSK3, HSK4, HSK5, HSK6
- **KR (ภาษาเกาหลี):** TOPIK1, TOPIK2, TOPIK3, TOPIK4, TOPIK5, TOPIK6

### 2. **LessonPreloader** (`lib/services/lesson_preloader.dart`)
Service สำหรับสร้างบทเรียนล่วงหน้า

**Features:**
- ✅ สร้างบทเรียนทั้งหมดสำหรับทุกภาษาและระดับ
- ✅ บันทึกเป็น JSON files
- ✅ รองรับการสร้างแบบ Manual และ Auto

### 3. **JSON Files Structure**

```
assets/data/
├── japanese_lessons.json (เดิม)
├── jp_n5_lessons.json (ใหม่)
├── jp_n4_lessons.json (ใหม่)
├── en_beginner_lessons.json (ใหม่)
├── en_intermediate_lessons.json (ใหม่)
├── cn_hsk1_lessons.json (ใหม่)
├── kr_topik1_lessons.json (ใหม่)
└── ...
```

## 🚀 วิธีใช้งาน

### **วิธีที่ 1: ใช้ LessonManager (แนะนำ)**

```dart
// โหลดบทเรียนตามภาษาและระดับ
final questions = await LessonManager.loadLessonsByLanguageAndLevel(
  language: 'JP',
  level: 'N5',
);

// ดึงรายการบทเรียน
final lessonList = LessonManager.getLessonList(
  language: 'JP',
  level: 'N5',
);
```

### **วิธีที่ 2: ใช้ LessonDataService**

```dart
// โหลดบทเรียนตามภาษาและระดับ
final questions = await LessonDataService.loadLessonsByLanguageAndLevel(
  language: 'JP',
  level: 'N5',
);
```

### **วิธีที่ 3: สร้างบทเรียนอัตโนมัติ**

```dart
// สร้างบทเรียนจากคำศัพท์ที่ใช้บ่อย
final lesson = await AutoLessonGenerator.generateLessonFromCommonWords(
  language: 'JP',
  level: 'N5',
  topic: 'คำทักทายพื้นฐาน',
  wordCount: 10,
);
```

## 📝 ตัวอย่าง JSON Structure

```json
{
  "language": "JP",
  "level": "N5",
  "lessons": [
    {
      "id": 1,
      "title": "คำทักทายพื้นฐาน",
      "level": "N5",
      "language": "JP",
      "questions": [
        {
          "question": "คำว่า 'สวัสดี' ภาษาญี่ปุ่นคือ?",
          "options": ["Sayonara", "Konnichiwa", "Arigatou", "Sumimasen"],
          "correctAnswerIndex": 1,
          "explanation": "Konnichiwa (こんにちは) แปลว่า 'สวัสดี'",
          "type": "multipleChoice"
        }
      ]
    }
  ]
}
```

## 🎯 Logic Flow

### **1. เมื่อผู้ใช้เลือกภาษาและระดับ:**
```
UserData.targetLanguage = "JP"
UserData.level = "N5"
↓
LessonManager.getLessonList(language: "JP", level: "N5")
↓
แสดงรายการบทเรียนใน lesson_list_page
```

### **2. เมื่อผู้ใช้เลือกบทเรียน:**
```
User clicks lesson ID 1
↓
LessonDetailPage loads
↓
LessonManager.loadLessonsByLanguageAndLevel(language: "JP", level: "N5")
↓
แสดงคำถามจากบทเรียน ID 1
```

### **3. Fallback Logic:**
```
1. ลองโหลดจาก JSON file ตามภาษาและระดับ
2. ถ้าไม่มี → ลองโหลดจาก japanese_lessons.json (เดิม)
3. ถ้ายังไม่มี → สร้างอัตโนมัติจาก API
4. ถ้ายังไม่ได้ → ใช้ข้อมูล default
```

## 📚 บทเรียนที่สร้างไว้แล้ว

### **ภาษาญี่ปุ่น (JP)**
- ✅ **N5:** 5 บทเรียน (คำทักทาย, ตัวเลข, สี, ครอบครัว, อาหาร)
- ⏳ **N4:** กำลังสร้าง
- ⏳ **N3:** กำลังสร้าง

### **ภาษาอังกฤษ (EN)**
- ✅ **Beginner:** 3 บทเรียน (Greetings, Numbers, Family)
- ⏳ **Intermediate:** กำลังสร้าง
- ⏳ **Advanced:** กำลังสร้าง

### **ภาษาจีน (CN)**
- ✅ **HSK1:** 2 บทเรียน (คำทักทาย, ตัวเลข)
- ⏳ **HSK2+:** กำลังสร้าง

### **ภาษาเกาหลี (KR)**
- ✅ **TOPIK1:** 2 บทเรียน (คำทักทาย, ตัวเลข)
- ⏳ **TOPIK2+:** กำลังสร้าง

## 🔧 การเพิ่มบทเรียนใหม่

### **วิธีที่ 1: เพิ่มใน JSON File**

1. เปิดไฟล์ `assets/data/{language}_{level}_lessons.json`
2. เพิ่มบทเรียนใหม่ใน array `lessons`
3. ใช้ ID ที่ไม่ซ้ำ

### **วิธีที่ 2: ใช้ Script**

```bash
# รัน script สร้างบทเรียนทั้งหมด
dart scripts/generate_all_lessons.dart
```

### **วิธีที่ 3: ใช้ UI ในแอป**

1. ไปที่ Settings → "ทดสอบ API & สร้างบทเรียน"
2. กด "สร้างบทเรียนจากคำศัพท์ที่ใช้บ่อย"
3. Copy JSON ที่ได้ไปใส่ในไฟล์

## 🎨 UI Integration

### **LessonListPage**
- ✅ แสดงบทเรียนตามภาษาและระดับที่เลือก
- ✅ ใช้ `LessonManager.getLessonList()` เพื่อดึงรายการ
- ✅ แสดง progress และ lock status

### **LessonDetailPage**
- ✅ โหลดคำถามตามภาษาและระดับ
- ✅ ใช้ `LessonManager.loadLessonsByLanguageAndLevel()`
- ✅ รองรับ fallback หลายระดับ

## 📊 สถานะปัจจุบัน

- ✅ **ระบบจัดการบทเรียน:** พร้อมใช้งาน
- ✅ **JSON Files:** สร้างไว้แล้วสำหรับ N5, Beginner, HSK1, TOPIK1
- ✅ **Auto Generation:** พร้อมใช้งาน
- ⏳ **บทเรียนเพิ่มเติม:** กำลังสร้าง

## 🚀 Next Steps

1. **เพิ่มบทเรียนให้ครบทุกระดับ**
   - ใช้ `LessonPreloader.preloadAllLessons()`
   - หรือใช้ script `generate_all_lessons.dart`

2. **เชื่อมต่อกับ UserData.level**
   - ดึงระดับจาก `UserData.level` แทนการใช้ default
   - ให้ผู้ใช้เลือกระดับได้ใน Settings

3. **เพิ่ม Progress Tracking ตามระดับ**
   - แยก progress ตามภาษาและระดับ
   - แสดง progress ใน UI

---

## 💡 Tips

- ใช้ `LessonManager.hasLessonsForLevel()` เพื่อตรวจสอบว่ามีบทเรียนหรือไม่
- ใช้ `LessonManager.getLevelsForLanguage()` เพื่อดึงระดับทั้งหมดสำหรับภาษา
- บทเรียนจะถูกโหลดอัตโนมัติตาม `UserData.targetLanguage` และระดับที่เลือก


