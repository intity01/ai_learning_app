# 💳 คู่มือการจัดการ Billing สำหรับ Hackathon

## 🎯 สถานการณ์
- Hackathon ต้องการ **Google Cloud service** (Gemini/Vertex AI)
- Google AI Studio ต้องเปิด **billing account** (แต่มี free tier)
- ไม่ต้องการจ่ายเงินจริง

---

## ✅ ทางเลือกที่ 1: ใช้ Google AI Studio Free Tier (แนะนำ)

### ข้อดี:
- ✅ ผ่านข้อกำหนด hackathon (เป็น Google Cloud service)
- ✅ มี free tier ที่ไม่ต้องจ่ายเงิน
- ✅ ตั้งค่าได้ง่าย

### Free Tier Limits:
- **Rate Limit:** ~15 requests/minute
- **Daily Limit:** ~1,500 requests/day
- **Monthly Limit:** ~60 requests/minute (ถ้าใช้มาก)

### วิธีตั้งค่า:
1. ไปที่ https://aistudio.google.com/app/apikey
2. Sign in ด้วย Google Account
3. กด "Create API Key"
4. **เปิด billing account** (จำเป็น แต่ไม่ต้องจ่ายถ้าใช้ไม่เกิน quota)
5. Copy API Key

### ⚠️ ข้อควรระวัง:
- ตั้งค่า **budget alerts** ใน Google Cloud Console
- ตั้งค่า **spending limit** เพื่อป้องกันค่าใช้จ่ายเกิน
- ตรวจสอบ usage เป็นประจำ

---

## ✅ ทางเลือกที่ 2: ขอ Google Cloud Credits จาก Hackathon

### ข้อมูลจากกฎ Hackathon:
- Hackathon มี **$50 Google Cloud credits** ให้
- สามารถขอได้โดยกรอกฟอร์มใน Devpost
- Credits จะอนุมัติภายใน 1-5 วันทำการ

### วิธีขอ:
1. ไปที่หน้า hackathon ใน Devpost
2. หา section "Google Cloud Credits" หรือ "Resources"
3. กรอกฟอร์มขอ credits
4. รอการอนุมัติ (1-5 วันทำการ)

### ข้อดี:
- ✅ มี credits เพียงพอสำหรับ hackathon
- ✅ ไม่ต้องกังวลเรื่องค่าใช้จ่าย
- ✅ ใช้ได้กับ Vertex AI หรือ Google AI Studio

---

## ✅ ทางเลือกที่ 3: ใช้ Mock Data สำหรับ Demo

### เมื่อไหร่ควรใช้:
- ไม่สามารถเปิด billing account ได้
- ต้องการทำ demo video ก่อน
- ต้องการทดสอบ UI/UX

### วิธีทำ:
1. สร้าง mock responses ใน `ai_service.dart`
2. ใช้สำหรับ demo video
3. ระบุใน submission ว่าใช้ mock data
4. เปลี่ยนเป็น real API เมื่อพร้อม

### ตัวอย่าง Mock Service:
```dart
// lib/services/ai_service_mock.dart
class AIServiceMock extends AIService {
  @override
  Future<String> sendMessage(String message, {...}) async {
    // Return mock response
    return 'นี่คือ mock response สำหรับ demo';
  }
}
```

---

## 📊 เปรียบเทียบทางเลือก

| ทางเลือก | ข้อดี | ข้อเสีย | เหมาะกับ |
|---------|-------|--------|---------|
| **Free Tier** | ฟรี, ง่าย, ผ่านข้อกำหนด | ต้องเปิด billing | ✅ แนะนำ |
| **Hackathon Credits** | มี credits เพียงพอ | ต้องรออนุมัติ | ✅ ถ้ามี |
| **Mock Data** | ไม่ต้องจ่าย, ทดสอบได้ | ไม่ผ่านข้อกำหนดจริง | ⚠️ เฉพาะ demo |

---

## 🎯 คำแนะนำ

### สำหรับ Hackathon:
1. **ลองขอ credits จาก hackathon ก่อน** (ถ้ามี)
2. **ถ้าไม่ได้ ใช้ free tier** (เปิด billing แต่ตั้งค่า budget alerts)
3. **ใช้ mock data สำหรับ demo** (ถ้ายังไม่ได้ API key)

### การตั้งค่า Budget Alerts:
1. ไปที่ Google Cloud Console
2. ไปที่ "Billing" → "Budgets & alerts"
3. สร้าง budget alert ที่ $0 (หรือ $1)
4. ตั้งค่า email notifications

---

## ⚠️ ข้อควรระวัง

1. **ตั้งค่า Budget Alerts** - เพื่อป้องกันค่าใช้จ่ายเกิน
2. **ตรวจสอบ Usage** - ตรวจสอบ usage เป็นประจำ
3. **ปิด Services หลัง Hackathon** - ปิด services ที่ไม่ใช้แล้ว
4. **อ่าน Terms** - อ่าน terms และ conditions ของ free tier

---

## 📝 Checklist

- [ ] ลองขอ Google Cloud credits จาก hackathon
- [ ] ตั้งค่า Google AI Studio API key
- [ ] ตั้งค่า budget alerts
- [ ] ทดสอบ API ทำงานได้
- [ ] ตรวจสอบ usage เป็นประจำ

---

**หมายเหตุ:** Free tier ของ Google AI Studio มี quota ที่เพียงพอสำหรับ hackathon แต่ควรตั้งค่า budget alerts เพื่อความปลอดภัย


