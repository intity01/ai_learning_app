# ⚡ คู่มือทดสอบ Error Feedback แบบเร็ว

## 🚀 วิธีรันแอป

```bash
# 1. เปิด Emulator (ถ้ายังไม่เปิด)
flutter emulators --launch pixel_6_-_api_34_naphat

# 2. รันแอป
flutter run -d emulator-5554
```

---

## ✅ ทดสอบ 3 ขั้นตอน

### **1. ตอบผิด → ดู Explanation**
1. ไปที่ **"บทเรียน"** → เลือก **"บทที่ 1"**
2. เลือกคำตอบ **ผิด** (เช่น เลือก "Sayonara" แทน "Konnichiwa")
3. กด **"ตรวจคำตอบ"**
4. **ควรเห็น:**
   - ✅ Modal Bottom Sheet แสดงขึ้นมาทันที
   - ✅ แสดงคำตอบที่ถูกต้อง + คำอธิบาย
   - ✅ ปุ่ม "เข้าใจแล้ว ไปต่อเลย"
5. กด **"เข้าใจแล้ว ไปต่อเลย"** → ไปข้อถัดไป

### **2. ตอบถูก → ไปต่อเลย**
1. เลือกคำตอบ **ถูก** (เช่น เลือก "Konnichiwa")
2. กด **"ตรวจคำตอบ"**
3. **ควรเห็น:**
   - ✅ คำตอบเป็นสีเขียว + icon ✓
   - ✅ **ไม่มี Modal** (เพราะตอบถูก)
   - ✅ ปุ่ม "ไปต่อเลย"
4. กด **"ไปต่อเลย"** → ไปข้อถัดไป

### **3. ทดสอบหลายข้อ**
1. ตอบผิด → ดู explanation → ไปต่อ
2. ตอบถูก → ไปต่อ
3. ตอบผิด → ดู explanation → ไปต่อ
4. ไปเรื่อยๆ จนจบบทเรียน

---

## 🎯 ผลลัพธ์ที่คาดหวัง

### ✅ เมื่อตอบผิด:
- Modal แสดงทันที
- แสดงคำตอบที่ถูกต้อง (สีเขียว)
- แสดงคำอธิบาย (สีฟ้า)
- ปุ่ม "เข้าใจแล้ว ไปต่อเลย"

### ✅ เมื่อตอบถูก:
- ไม่มี Modal
- คำตอบเป็นสีเขียว
- ปุ่ม "ไปต่อเลย"

---

## 🐛 ถ้ามีปัญหา

### Modal ไม่แสดง:
- ตรวจสอบว่า `_hasShownExplanation` ถูก reset ใน `_nextQuestion()`

### Modal แสดงซ้ำ:
- ตรวจสอบว่า `_showExplanationDialog()` มี `if (_hasShownExplanation) return;`

---

**พร้อมทดสอบ! 🎉**


